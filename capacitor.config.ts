import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.geocoder.v1',
  appName: 'cs-geocoder-1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
